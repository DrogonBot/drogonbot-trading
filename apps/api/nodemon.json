{
  "watch": [
    "../../apps/**/src/**/*.ts",
    "../../packages/**/src/**/*.ts"
  ],
  "ext": "ts",
  "execMap": {
    "ts": "lerna run build --since master --stream --ignore @drogonbot/api && ts-node src/index.ts"
  }
}